<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2000/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>liuyuyang.net</groupId>
    <artifactId>ThriveX_Server</artifactId>
    <version>4.0</version>

    <packaging>pom</packaging>

    <modules>
        <!-- 实体类 -->
        <module>model</module>
        <!-- 核心业务 -->
        <module>blog</module>
    </modules>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <properties>
        <java.version>21</java.version>
        <spring-boot.version>3.5.16</spring-boot.version>
        <mybatis-plus.version>3.5.17</mybatis-plus.version>
        <!-- MySQL 驱动保持项目原有的 8.0.x 版本（Connector/J 9.x 对旧版 MySQL 服务端兼容性欠佳） -->
        <mysql.version>8.0.23</mysql.version>
        <!-- knife4j 4.5.0 与 Boot 3.5 不兼容，已移除，API 文档改用 springdoc -->
        <springdoc.version>2.8.17</springdoc.version>
        <!-- 与 springdoc 2.8.x 使用的 swagger-annotations 保持一致 -->
        <swagger-annotations.version>2.2.39</swagger-annotations.version>
        <jjwt.version>0.12.7</jjwt.version>
        <rome.version>2.1.0</rome.version>
        <oshi.version>6.12.0</oshi.version>
        <qiniu.version>7.13.0</qiniu.version>
        <bucket4j.version>8.10.1</bucket4j.version>
    </properties>

    <!--配置dependencyManagement锁定依赖的版本-->
    <dependencyManagement>
        <dependencies>
            <!-- spring boot热重载 -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-devtools</artifactId>
                <version>${spring-boot.version}</version>
                <scope>runtime</scope>
            </dependency>

            <!-- 引入aop切面支持 -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-aop</artifactId>
                <version>${spring-boot.version}</version>
            </dependency>

            <!-- mybatis-plus 持久层（Boot 3 专用坐标） -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <!-- 3.5.9 起从核心包拆分：ServiceImpl 等 extension 能力在此构件 -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-extension</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <!-- 3.5.9 起分页等插件需要显式引入 jsqlparser 适配 -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <!-- MySQL 驱动（还原项目原版本，Boot 3 不再托管旧坐标） -->
            <dependency>
                <groupId>mysql</groupId>
                <artifactId>mysql-connector-java</artifactId>
                <version>${mysql.version}</version>
            </dependency>

            <!-- Scalar：更现代的文档 UI，入口 /scalar -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-scalar</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- v3 版 OpenAPI 注解（@Schema/@Tag/@Operation） -->
            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-annotations-jakarta</artifactId>
                <version>${swagger-annotations.version}</version>
            </dependency>

            <!-- jwt（0.12 起拆分为 api/impl/jackson 三个包，且仅支持 jakarta 与强秘钥） -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>

            <dependency>
                <groupId>com.rometools</groupId>
                <artifactId>rome</artifactId>
                <version>${rome.version}</version>
            </dependency>

            <dependency>
                <groupId>com.github.oshi</groupId>
                <artifactId>oshi-core</artifactId>
                <version>${oshi.version}</version>
            </dependency>

            <!-- 七牛云对象存储 -->
            <dependency>
                <groupId>com.qiniu</groupId>
                <artifactId>qiniu-java-sdk</artifactId>
                <version>${qiniu.version}</version>
            </dependency>

            <!-- 自定义模块 -->
            <dependency>
                <groupId>liuyuyang.net</groupId>
                <artifactId>model</artifactId>
                <version>4.0</version>
            </dependency>

            <!-- 基于令牌桶算法的限流库（仅引核心包，限流切面为手写实现，无需 starter） -->
            <dependency>
                <groupId>com.bucket4j</groupId>
                <artifactId>bucket4j-core</artifactId>
                <version>${bucket4j.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
